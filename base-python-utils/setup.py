from setuptools import setup

REQUIRES = []


setup(
    name="base-utils",
    version="0.1.0",
    description="Simple logger",
    packages=["base_utils"],
    install_requires=REQUIRES,
    python_requires=">=3.6",
)
